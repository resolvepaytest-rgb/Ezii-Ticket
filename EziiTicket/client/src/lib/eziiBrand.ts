/**
 * Ezii product branding (aligned with design tokens).
 * Keep HSL values in `index.css` [data-brand="ezii"] in sync when these change.
 */
export const EZII_BRAND = {
  primary: "#1E88E5",
  /** Lighter primary for charts / secondary bars */
  primaryLight: "#42A5F5",
  secondary: "#475569",
  tertiary: "#CC6C00",
  neutral: "#F8FAFC",
} as const;
